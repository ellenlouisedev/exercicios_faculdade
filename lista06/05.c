// 5. Faça um programa que receba dez números inteiros e mostre a quantidade de números primos dentre os números que foram digitados.

#include <stdio.h>

int main(){
    int cont = 0, num;
    scanf("%d", &num);
    while (cont < 10)
    {
        scanf("%d", &num);

    }
    
    return 0;
}